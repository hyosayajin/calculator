export default class DME {
	// # Basic html elements
	static body = document.querySelector("body");
	static app = document.getElementById("app");

	// # Default html elements
	static header = document.getElementById("header");
	static navigationHeaderTriggerButton = document.getElementById("navigationHeaderTriggerButton");
	static headerNavigation = document.getElementById("headerNavigation");
	static headerNavigationList = document.getElementById("headerNavigationList");

	// # Default short cut html elements
	// static shortCut = document.getElementById("shortCut");
	// static helper = document.getElementById("helper");
	// static parameter = document.getElementById("parameter");

	// static shortCutButtons = Array.from(this.shortCut.querySelectorAll("button"));

	// static helperButton = document.getElementById("helperButton");
	// static parameterButton = document.getElementById("parameterButton");

	// # Default html elements, all select
	// static options = Array.from(document.querySelectorAll(".ops-button"));

	// # project html elements, all select

	// # Project html elements
	static calculatorWrapper = document.getElementById("calculatorWrapper");
	static adder = document.getElementById("adder");
	static minus = document.getElementById("minus");

}
